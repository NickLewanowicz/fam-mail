{
  "name": "fam-mail",
  "version": "1.0.0",
  "private": true,
  "description": "A simple wrapper around PostGrid API for sending postcards to friends and family",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter backend start",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "keywords": ["postgrid", "postcard", "mail", "bun", "vite"],
  "author": "nicklewanowicz",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
